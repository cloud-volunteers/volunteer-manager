services:
  volunteer-manager:
    image: volunteer-manager
    container_name: volunteer-manager
    build:
      context: .
    command: uvicorn server:app --host 0.0.0.0 --port 2137
    env_file:
      - .env
    environment:
      - APP_ENV=DOCKER
    ports:
      - 2137:2137